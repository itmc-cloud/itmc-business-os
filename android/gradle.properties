org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
# If Gradle downloads fail TLS validation on your machine (common with
# antivirus HTTPS scanning, e.g. Avast), that's a local JVM truststore issue
# — fix it in your own ~/.gradle/gradle.properties (not this file, which is
# shared across every machine/CI that builds this project) by adding:
#   org.gradle.jvmargs=... -Djavax.net.ssl.trustStore="<path-to-your-truststore>" -Djavax.net.ssl.trustStorePassword=changeit
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
